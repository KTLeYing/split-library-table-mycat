# ??????
server:
  port: 8080
  servlet:
    context-path: /splitByMycat

spring:
  # MySQL?????
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 105293
    url: jdbc:mysql://localhost:3306/java_stack_study?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC&allowPublicKeyRetrieval=true
  activemq:
    broker-url:

#mybatis-plus??
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.mzl.splitlibrarytablemycat.entity
  type-enums-package: com.mzl.splitlibrarytablemycat.enums
  # MyBatis ???????
  configuration:
    # ?????????????camel case???
    mapUnderscoreToCamelCase: true
    # ??????SQL?????????
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    # ??????SQL???????
#    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  # ??????
  global-config:
    db-config:
      # ????????????(since 3.3.0,????????????2)
      logic-delete-field: deleted
      # ??????(??? 1)
      logic-delete-value: 0
      # ??????(??? 0)
      logic-not-delete-value: 1
      # ???????
      select-strategy: not_empty